import { Enemy } from '../entities/Enemy';
import { Tower } from '../entities/Tower';

export function updateCombat(towers: Tower[], enemies: Enemy[], deltaTime: number): void {
  // Combat system logic
}
